# Self-Improve Entry

- 日期：2026-02-16
- 触发来源：TASK-20260216-120619
- 风险等级：R2

## 问题

- `new-task` 的参数与文档不一致：`README.md`/usage 提到 `--full`，但 `.team-os/scripts/new_task.sh` 仅支持 `--short`，按文档执行会报错。
- Runtime 模板供应链与宿主机风险偏高：`.team-os/templates/runtime/docker-compose.yml` 中 OpenHands 使用漂移 tag（`latest-*`）且默认挂载 `/var/run/docker.sock`。

## 改进方案

- 修正 `new-task` 一致性与兼容性（已在本次任务中完成）：
  - `.team-os/scripts/new_task.sh` 支持 `--full` 作为兼容别名
  - `.team-os/scripts/teamos.sh` usage 更新
  - `README.md` 快速开始示例更新
- 收敛 Runtime 默认风险面（待办）：
  - 固定 OpenHands 镜像版本 tag（避免 `latest` 漂移）
  - 将 docker socket 挂载改为默认关闭，改用 compose override 或显式 gate 开启
  - 在 `docs/SECURITY.md`/`docs/EXECUTION_RUNBOOK.md` 补齐该默认策略说明

## 验收标准

- `./scripts/teamos.sh new-task --full "<title>"` 不再报错，且行为与默认一致（生成 00~07 + metrics）。
- `README.md` 与 `./scripts/teamos.sh` usage 不再出现与脚本实现矛盾的参数说明。
- Runtime 模板不再使用漂移 tag，且默认不挂载 docker socket（需要显式启用）。

## 证据链接

- `README.md`
- `.team-os/scripts/teamos.sh`
- `.team-os/scripts/new_task.sh`
- `.team-os/templates/runtime/docker-compose.yml`
- `.team-os/logs/tasks/TASK-20260216-120619/03_work.md`

## issue/PR

- issue:
- pr:
