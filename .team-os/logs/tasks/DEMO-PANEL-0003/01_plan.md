# DEMO-PANEL-0003 Plan

- 需求冲突进入 `NEED_PM_DECISION`（requirements.yaml）
- 同步策略：
  - task: `need_pm_decision=true` -> 字段 `Need PM Decision = Yes`
  - requirement: `status=NEED_PM_DECISION` -> 生成 `DECISION:<REQ>` item

